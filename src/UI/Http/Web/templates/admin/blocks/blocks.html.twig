{% extends '@MainBundle/admin/layouts/base.html.twig' %}

{% block title %}{{ 'zentlix_block.text_blocks' | trans }}{% endblock %}

{% block breadcrumbs %}
    <li class="breadcrumb-item"><a href="{{ path('admin.index') }}">{{ 'zentlix_main.control_panel' | trans }}</a></li>
    <li class="breadcrumb-item active">{{ 'zentlix_block.text_blocks' | trans }}</li>
{% endblock %}

{% block body %}
    <div class="row">
        <div class="card w-100">
            <div class="card-header">
                {{ 'zentlix_block.text_blocks' | trans }}
                <a href="{{ path('admin.block.create') }}" class="btn btn-primary btn-sm float-right">
                    {{ 'zentlix_block.create.action' | trans }}
                </a>
            </div>
            <div class="card-body">
                <div id="blocks-datatable">{{ 'zentlix_main.loading_dots' | trans }}</div>
                <script>
                    $(document).ready(function () {
                        $('#blocks-datatable').initDataTables({{ datatable_settings(datatable) }});
                    });
                </script>
            </div>
        </div>
    </div>
{% endblock %}